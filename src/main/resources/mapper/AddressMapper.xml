<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyh.cstore.mapper.AddressMapper">
    <resultMap id="addressMap" type="com.hyh.cstore.entity.Address">
        <id column="aid" property="aid"/>
        <result column="province_code" property="provinceCode"/>
        <result column="province_name" property="provinceName"/>
        <result column="city_code" property="cityCode"/>
        <result column="city_name" property="cityName"/>
        <result column="area_code" property="areaCode"/>
        <result column="area_name" property="areaName"/>
        <result column="is_default" property="isDefault"/>
        <result column="created_user" property="createdUser"/>
        <result column="created_time" property="createdTime"/>
        <result column="modified_user" property="modifiedUser"/>
        <result column="modified_time" property="modifiedTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="aid">
        INSERT INTO store.t_address (
            uid, name, province_name, province_code, city_name, city_code, area_name, area_code, zip,
            address, phone, tel,tag, is_default, created_user, created_time, modified_user, modified_time
        ) VALUES (
                     #{uid}, #{name}, #{provinceName}, #{provinceCode}, #{cityName}, #{cityCode}, #{areaName},
                     #{areaCode}, #{zip}, #{address}, #{phone}, #{tel}, #{tag}, #{isDefault}, #{createdUser},
                     #{createdTime}, #{modifiedUser}, #{modifiedTime}
                 )
    </insert>
    <select id="countByUid" resultType="java.lang.Integer">
        SELECT count(*) from store.t_address WHERE uid=#{uid}
    </select>
    <select id="findNameByCode" resultType="java.lang.String">
        SELECT name
        FROM store.t_dict_district
        WHERE code=#{code}
    </select>
    <select id="findByUid" resultMap="addressMap">
        SELECT *
        FROM store.t_address
        WHERE uid=#{uid}
        ORDER BY is_default DESC , aid DESC
    </select>
    <select id="findByAid" resultMap="addressMap">
        SELECT * FROM store.t_address WHERE aid=#{aid}
    </select>
    
    <update id="updateNonDefaultByUid">
        UPDATE store.t_address SET is_default=0 WHERE uid=#{uid}
    </update>
    <update id="updateDefaultByAid">
        UPDATE store.t_address SET is_default=1 WHERE aid=#{aid}
    </update>

    <delete id="deleteByAid">
        DELETE FROM store.t_address WHERE aid=#{aid}
    </delete>

    <select id="findLastModified" resultMap="addressMap">
        SELECT * FROM store.t_address WHERE uid=#{uid} ORDER BY modified_time DESC LIMIT 0,1
    </select>
</mapper>